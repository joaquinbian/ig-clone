import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  indexContainer: {
    position: 'absolute',
    bottom: 10,
    alignSelf: 'center',
    flexDirection: 'row',
  },
  circle: {
    height: 10,
    aspectRatio: 1,
    borderRadius: 5,
    marginHorizontal: 5,
  },
});
